{
  "name": "skema-monorepo",
  "version": "0.0.2",
  "private": true,
  "workspaces": [
    "packages/skema-core",
    "packages/skema-example"
  ],
  "scripts": {
    "dev": "bun run build && (bun run --filter skema-core watch & bun run --filter skema-example dev)",
    "build": "bun run --filter skema-core build",
    "example": "bun run build && bun run --filter skema-example dev",
    "serve": "bun packages/skema-core/dist/cli.js",
    "skema": "node packages/skema-core/dist/cli.js",
    "skema:init": "node packages/skema-core/dist/cli.js init"
  }
}
