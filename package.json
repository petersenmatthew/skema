{
  "name": "skema-monorepo",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "packages/skema-core",
    "packages/skema-example"
  ],
  "scripts": {
    "dev": "bun run build && (bun run --filter skema-core watch & bun run --filter skema-example dev)",
    "build": "bun run --filter skema-core build",
    "example": "bun run build && bun run --filter skema-example dev"
  }
}
